# yaml 파일은 띄어쓰기 기준으로 설정하는 파일이다.
# 띄어쓰기 기준으로 부모자식간의 관계가 형성된다.

schema: http://backend-example.codebootcamp.co.kr/graphql
generates:
  ./src/commons/types/generated/types.ts:
    plugins:
      - typescript
    config:
      typesPrefix: I
      # type앞에 I가 붙는다. (interface처럼)
